---
type: memory
title: iBridge V1.0 补齐批次 1（F3/F4/F6/F7）2026-09-13
created: 2026-09-13T07:42:07.456875+00:00
source: cli
---

# iBridge V1.0 补齐批次 1（F3/F4/F6/F7）2026-09-13

# V1.0 补齐批次 1

已完成并验证：
- F7：iOS `scenePhase` 检测 → 回前台 toast「视频在后台暂停，现已恢复」。
- F6：`StreamRecorder` 改为 `AVAssetWriter` 单文件 mux（H.264 + AAC），用 CMSampleBufferCreateReady
  从 Int16 PCM 造 audio sample；ffprobe 确认双轨，不再有 .wav。
- F4：演示模式（`ibridge.ios.demoMode`）——`DemoCameraView` 水印画面 + 顶栏「演示」徽标；
  demo 时 `statusBanner` 不弹离线 alert。
- F3：iPhone 固定端口 8765（`NWListener(using:on:)` 失败回退动态）+ `getifaddrs(en0)` 取本机 IP；
  连接页显示 `IP:port`；Mac `ReceiverSession.connectManually(host:port:)` + 菜单「手动连接…」alert（`DiscoveredPhone` 早有裸 host:port 路径）。

未做（大，系统级）：
- F2 虚拟麦克风 CoreAudio HAL `AudioServerPlugin`（装 /Library/Audio/Plug-Ins/HAL，需 sudo + 签名）。
- F1 Camera Extension：CMIO sysex 已搭骨架，需用户在系统设置激活 + Photo Booth/Zoom 真机验证帧投递。

验证：`./scripts/test.sh` 74 全绿；`./scripts/e2e-device.sh` 10/10；录制 mov 双轨（ffprobe）。
