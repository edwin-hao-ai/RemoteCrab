[package]
name = "rc-testkit"
description = "A fake iPhone (RemoteCrab iOS sender) for testing the Windows receiver end to end."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
rc-protocol.workspace = true
tokio.workspace = true
# Only used to synthesise real H.264 for the preview self-test.
openh264 = "0.9"
